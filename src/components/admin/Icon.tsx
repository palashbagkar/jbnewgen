/** Replaces the Payload mark in the admin nav header. */
export function AdminIcon() {
  return (
    /* eslint-disable-next-line @next/next/no-img-element */
    <img
      src="/images/logo-CMS.png"
      alt="JB NewGen"
      style={{ width: 'auto', maxWidth: '9rem', height: 'auto', maxHeight: '2rem' }}
    />
  )
}

export default AdminIcon
