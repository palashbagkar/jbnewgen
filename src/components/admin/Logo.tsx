/** Replaces the Payload wordmark on the admin login screen. */
export function AdminLogo() {
  return (
    /* eslint-disable-next-line @next/next/no-img-element */
    <img
      src="/images/logo-CMS.png"
      alt="JB NewGen"
      style={{ width: 'auto', maxWidth: '18rem', height: 'auto', maxHeight: '5rem' }}
    />
  )
}

export default AdminLogo
